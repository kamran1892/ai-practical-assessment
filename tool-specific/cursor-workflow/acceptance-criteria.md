# Acceptance Criteria (Cursor working copy)

Mirror of Core criteria — keep in sync with root `acceptance-criteria.md`.

Coverage tags: **Frontend** | **Backend** | **DB** | **Tests** | **Docs**

- [ ] Create ticket via UI — **Frontend + Backend + DB**
- [ ] List tickets from DB — **Frontend + Backend + DB**
- [ ] Ticket detail view — **Frontend + Backend + DB**
- [ ] Update title, description, priority, assignee — **Frontend + Backend + DB**
- [ ] Add comments — **Frontend + Backend + DB**
- [ ] Valid status transitions only; invalid rejected — **Frontend + Backend + DB + Tests**
- [ ] Keyword search + status filter — **Frontend + Backend + DB**
- [ ] Data persists after restart — **DB**
- [ ] Backend validation — **Backend** (+ DB constraints/FKs)
- [ ] No secrets in repo — **Docs** / repo hygiene
- [ ] State-machine integration tests pass — **Tests** (+ Backend + DB)
